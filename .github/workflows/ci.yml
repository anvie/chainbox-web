name: deploy
on:
  push:
    branches:
      - main
      - master
  # schedule:
  #   - cron: '0 0 * * *' # Everyday at 12am
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      API_BASE_URL: ${{ secrets.API_BASE_URL }}
      BASE_URL_PROJECT_DATA_DIR: ${{ secrets.BASE_URL_PROJECT_DATA_DIR }}
    steps:
      - uses: actions/checkout@v2

      - name: Setup node env 🏗
        uses: actions/setup-node@v2.5.1
        with:
          node-version: ${{ matrix.node }}
          check-latest: true

      - run: yarn install
      - run: yarn generate

      - name: Install SSH Key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          known_hosts: unnecessary

      - name: Adding Known Hosts
        run: ssh-keyscan -p ${{ secrets.SSH_PORT}} -H ${{ secrets.SSH_HOST }}  >> ~/.ssh/known_hosts

      - name: Deploy with rsync
        run: rsync -avzrhcP --chown=www:www -e "ssh -p ${{ secrets.SSH_PORT }}" ./out/ ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }}:/home/www/chainbox-frontend/
      
      # - name: Sync node_modules
      #   run: rsync -avzrhcP -e "ssh -p ${{ secrets.SSH_PORT }}" ./node_modules ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }}:/home/www/chainbox-frontend/

      # - name: Restart Node Server
      #   uses: appleboy/ssh-action@master
      #   with:
      #     host: ${{ secrets.SSH_HOST }}
      #     username: ${{ secrets.SSH_USERNAME }}
      #     key: ${{ secrets.SSH_PRIVATE_KEY }}
      #     port: ${{ secrets.SSH_PORT }}
      #     script: |
      #       sudo -u www pm2 restart frontend
